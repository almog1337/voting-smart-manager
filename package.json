{
  "name": "voting-smart",
  "version": "0.0.1",
  "private": true,
  "packageManager": "pnpm@10.33.0",
  "scripts": {
    "build": "turbo run build",
    "dev": "turbo run dev start:dev",
    "dev:api": "turbo run start:dev --filter=@voting-smart/api",
    "dev:client": "turbo run dev --filter=@voting-smart/client",
    "test": "turbo run test",
    "lint": "turbo run lint",
    "format": "turbo run format"
  },
  "devDependencies": {
    "turbo": "latest"
  }
}
